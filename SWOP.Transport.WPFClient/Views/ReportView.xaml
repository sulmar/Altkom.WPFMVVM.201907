<Page x:Class="SWOP.Transport.WPFClient.Views.ReportView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SWOP.Transport.WPFClient.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=ReportViewModel}"
      Title="ReportView">

    <Grid>
        <DockPanel>
            <StackPanel DockPanel.Dock="Top">
                <Label Content="{Binding Report.Description}" />
            </StackPanel>

            <DataGrid
                IsReadOnly="True"
                ItemsSource="{Binding Report.Details}" />

        </DockPanel>
        
    </Grid>
</Page>
